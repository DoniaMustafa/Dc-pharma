import '../../../core/app_export.dart';/// This class is used in the [merkcomponent5_item_widget] screen.
class Merkcomponent5ItemModel {Merkcomponent5ItemModel({this.groupIcon, this.companyName, this.itemCount, this.id, }) { groupIcon = groupIcon  ?? ImageConstant.imgGroup14;companyName = companyName  ?? "sanofi";itemCount = itemCount  ?? "601 Items";id = id  ?? ""; }

String? groupIcon;

String? companyName;

String? itemCount;

String? id;

 }
