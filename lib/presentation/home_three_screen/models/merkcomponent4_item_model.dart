import '../../../core/app_export.dart';/// This class is used in the [merkcomponent4_item_widget] screen.
class Merkcomponent4ItemModel {Merkcomponent4ItemModel({this.drugIcon, this.medicineText, this.itemText, this.id, }) { drugIcon = drugIcon  ?? ImageConstant.img3668843DrugsM;medicineText = medicineText  ?? "medicines";itemText = itemText  ?? "601 Items";id = id  ?? ""; }

String? drugIcon;

String? medicineText;

String? itemText;

String? id;

 }
